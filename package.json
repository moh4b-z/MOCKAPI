{
  "name": "mockapi",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "start": "json-server --watch fotos.json --port 3000"
  },
  "dependencies": {
    "json-server": "^0.17.3"
  }
}
